package com.gotimer.ui

import android.app.Activity
import android.os.Bundle

/**
 * Launcher activity placeholder for Phase 3.
 *
 * The full Compose dashboard lands in Phase 5; this stub keeps notification
 * tap-through and the launcher intent wired so the app can be installed and
 * opened.
 */
class MainActivity : Activity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
    }
}
